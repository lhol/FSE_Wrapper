Please create a file named v0.0.15_spec.md, plan the tasks needed for the following issues, track that down in the specs file and implement it:

Issues to fix:
Panama issues:
- documentation is written as if java-panama lib is not existing
- check if panama lib is working and if it can be used instead of JNI
- write a test for panama lib and add it to the CI
Benchmark issues:
- the benchmark should be updated to use panama lib instead of JNI
- the benchmark should use different test sizes (512 bytes, 1KB, 4KB, 16KB, 64KB, 256KB, 1MB, 4MB, 16MB) and two differnt types of entropy data (pure random makes no sense)
- please check the throughput numbers the benchmark outputs. 
- for java and .net the benchmark is giving no numbers in the markdown file. Please check if the benchmark is working and if it is not, fix it.
Versioning issues:
- Version numbers of jar files and nuget packages should reflect the current release version. 

Roadmap:
- create a v0.0.16_req.md file and v0.0.16_spec.md file and add tasks which should be done in the next release. Please add nuget packaging and maven packaging to the roadmap. The current release is not packaged and published to nuget.org and maven central.
 