# v0.0.15 Specification

## Overview

This release fixes several correctness and usability issues in the benchmarking infrastructure, the Panama FFI integration, and the artifact versioning.

---

## Tasks

### 1. Panama FFI Integration [DONE]
- [x] Add `java-panama/src/main/java` as Maven source directory
- [x] Upgrade Maven compiler target to Java 21 (required for `java.lang.foreign`)
- [x] Add `--enable-native-access=ALL-UNNAMED` to Surefire argLine and JMH fork args
- [x] Write `PanamaTests.java` for `Huff0Panama` (compress + decompress roundtrip)
- [x] Write `PanamaTests.java` for `FsePanama` (compress + decompress roundtrip)
- [x] Fix README: remove "hypothetical/future" framing; show actual Panama API usage
- [x] Add Panama benchmarks to JMH (alongside JNI benchmarks)

### 2. Benchmark Improvements [DONE]
- [x] Update `bench_codecs.c` to benchmark multiple sizes (512B→16MB) and two data types
- [x] Update JMH `CodeBench.java` (huff0 + fse) with `@Param` sizes and data types
- [x] Add Panama compress methods to JMH benchmarks
- [x] Update `.NET` `CodecBenchmarks.cs` (huff0 + fse) with `[Params]` for sizes and data types
- [x] Fix `.NET` benchmark `Main` to use `BenchmarkSwitcher` (CLI args were ignored)
- [x] Fix `generate_dashboard.py`: handle parameterized JMH JSON, glob for BDN JSON files
- [x] Update dashboard Markdown to show per-size table

### 3. Versioning [DONE]
- [x] Set `pom.xml` version to `0.0.15`
- [x] Pass version from Git tag to Maven (`mvn versions:set`) in release + rc workflows
- [x] NuGet version already pulled from tag (confirmed OK)

### 4. Workflow Fixes [DONE]
- [x] Fix `rc.yaml` bench job: artifact download name `native-linux-x64-` (not `native-linux-`)
- [x] Add `--enable-native-access=ALL-UNNAMED` JVM arg in release bench step for Panama JMH
- [x] Add checkout + submodules to rc bench (it was missing)

### 5. Roadmap [DONE]
- [x] Create `specs/v0.0.16_req.md` and `specs/v0.0.16_spec.md`

---

## Data Types for Benchmarks

| Name | Description | Compressibility |
|------|-------------|-----------------|
| `LOW_ENTROPY` | Repeating byte sequence `i % 32` | High — very compressible |
| `MEDIUM_ENTROPY` | Lorem Ipsum text repeated to size | Medium — like typical text data |

## Test Sizes

512B, 1KB, 4KB, 16KB, 64KB, 256KB, 1MB, 4MB, 16MB

---

## Release Checklist

- [ ] All local builds pass (`./build.ps1`)
- [ ] Panama tests pass
- [ ] JMH benchmarks compile and run
- [ ] .NET benchmarks compile and export JSON
- [ ] Dashboard script produces output with all benchmarks
- [ ] Tag `RC-v0.0.15` → verify RC workflow passes
- [ ] Tag `v0.0.15` → verify release workflow creates release with ZIPs
