# v0.1.1 Requirements

## Overview

v0.1.1 focuses on publishing the library to public package registries so users can consume FSE_Wrapper without building from source.

---

## Issues to Fix / Features to Add

### Maven Central Publishing
- Publish `org.karenta:FSE_Wrapper` JAR to Maven Central
- Set up OSSRH (Sonatype) account and signing keys
- Configure `maven-gpg-plugin` and `nexus-staging-maven-plugin` in pom.xml
- Add GitHub Actions workflow step that publishes on `v*.*.*` tag
- Include Javadoc and sources JAR (required by Maven Central)

### NuGet Publishing
- Publish `Huff0.Net` and `Fse.Net` to nuget.org
- Current release.yaml has NuGet publish step but OIDC trusted publishing needs configuration on nuget.org
- Document the exact nuget.org policy setup steps
- Test publishing with a staging package first

### Documentation / Infrastructure
- Add `CHANGELOG.md` with release notes per version
- Set up GitHub release notes automation from commit messages
- Add contributing guide (`CONTRIBUTING.md`)
- Verify that Javadoc is generated and correct for all public API

### Testing
- Add integration tests that download the published packages and verify they work
- Add smoke test for Panama FFI on each platform in the native CI matrix

### Versioning
- Introduce proper semantic versioning strategy (currently 0.0.x is a placeholder)
- Consider switching to 1.0.0 once Maven/NuGet publishing is live
