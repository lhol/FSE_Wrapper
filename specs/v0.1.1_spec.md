# v0.1.1 Specification

## Overview

Complete package registry publishing (Maven Central) and improve documentation/testing. NuGet is already live.

---

## Tasks

### 1. Maven Central Publishing
- [ ] Sign up / configure OSSRH Sonatype account at https://central.sonatype.com/
- [ ] Generate GPG signing key, store as GitHub secrets `GPG_PRIVATE_KEY` and `GPG_PASSPHRASE`
- [ ] Add to both `pom.xml` and `java-panama/pom.xml`:
  - `maven-gpg-plugin` (sign artifacts)
  - `maven-source-plugin` (attach sources JAR)
  - `maven-javadoc-plugin` (attach javadoc JAR)
  - Distribution management section with OSSRH URLs
  - Required metadata: `<licenses>`, `<developers>`, `<scm>`, `<description>`
- [ ] Add `release-maven.yaml` workflow that runs on `v*.*.*` tags:
  - Sets version from tag
  - Signs and uploads JNI JAR + Panama JAR to OSSRH staging
  - Promotes staging to release

### 2. NuGet Publishing — ALREADY WORKING ✅
- NuGet packages `Huff0.Net` and `Fse.Net` are published on every `v*.*.*` tag via `release.yaml`
- Uses a GitHub secret (API key) already configured in the repository
- **Action**: Document the existing credential/workflow setup in README so future maintainers understand it
- **Action**: Verify that `v0.0.15` packages appear correctly on nuget.org after release

### 3. CHANGELOG.md
- [ ] Create `CHANGELOG.md` with entries for v0.0.1 through current version
- [ ] `gh release create --generate-notes` already adds auto-generated notes per release — verify they are readable
- [ ] Add conventional commit format guidance to `CONTRIBUTING.md`

### 4. Javadoc + Sources
- [ ] Add Javadoc to all public methods in `Huff0.java`, `Fse.java`, `Huff0Panama.java`, `FsePanama.java`
- [ ] Verify `./mvnw javadoc:javadoc` and `./mvnw -f java-panama/pom.xml javadoc:javadoc` succeed
- [ ] Attach `-sources.jar` and `-javadoc.jar` to GitHub Release artifacts

### 5. Integration Smoke Tests
- [ ] After NuGet publish: minimal C# project consuming `Huff0.Net`, runs compress/decompress
- [ ] After Maven publish: minimal Java project consuming `FSE_Wrapper`, runs compress/decompress
- [ ] Run as separate CI jobs triggered after publish succeeds

---

## Notes

- Maven Central requires GPG signing — one-time account/key setup
- **NuGet is done** — no OIDC policy setup needed; the API key credential approach is already working
- Target: v0.1.1 ships both Maven Central + clearly documented NuGet, setting the stage for a 1.0.0 release

## Dependencies

- Requires v0.0.15 to be released and verified first
- Requires OSSRH account approval (can take 1-2 business days for new accounts)
