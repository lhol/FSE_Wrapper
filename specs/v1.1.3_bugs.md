The Following Issues were found:
1. The Nuget upload mechanism and the maven upload mechanism are different (extra job for maven). I would like it more to include the maven part into the deployment, but keep it "optional". Since there is a approval step before either of those jobs can access the environemt of "release", I can skip those two if I like to. Or is there a benefit of the maven job running seperately? My guess it it would save quite some time to do it in the main deployment. In any case, make sure that the Package publishing jobs do not stop the release as long as all zip files can be uploaded, which is correct at the moment since all those steps are behind the release step.
2. There is a bug in the smoke-nuget:
```terminaloutput
info : X.509 certificate chain validation will use the fallback certificate bundle at '/usr/share/dotnet/sdk/10.0.302/trustedroots/codesignctl.pem'.
info : X.509 certificate chain validation will use the fallback certificate bundle at '/usr/share/dotnet/sdk/10.0.302/trustedroots/timestampctl.pem'.
info : Adding PackageReference for package 'Huff0.Net' into project '/tmp/smoke-nuget/SmokeTest/SmokeTest.csproj'.
info : Restoring packages for /tmp/smoke-nuget/SmokeTest/SmokeTest.csproj...
info :   GET https://api.nuget.org/v3-flatcontainer/huff0.net/index.json
info :   OK https://api.nuget.org/v3-flatcontainer/huff0.net/index.json 41ms
error: NU1102: Unable to find package Huff0.Net with version (>= 0.1.2)
error:   - Found 3 version(s) in nuget.org [ Nearest version: 0.0.17 ]
error: Package 'Huff0.Net' is incompatible with 'all' frameworks in project '/tmp/smoke-nuget/SmokeTest/SmokeTest.csproj'.
Error: Process completed with exit code 1.
```
I would guess it could be, that the newly published version still needs some time to show up. Can we have a 15 min interleave?
3. The smoke test for maven is not yet implemented, but I would like to have it in the same way as the nuget smoke test. It should be a simple java project that uses the published maven artifacts and runs a simple compress/decompress test.
4. The step bench, in the release and in the release candidate actions have an error:
```terminaloutput
Warning: [WARNING] /home/runner/work/FSE_Wrapper/FSE_Wrapper/target/generated-sources/annotations/io/github/lhol/huff0/jmh_generated/CodeBench_jmhType_B3.java:2: warning: use of default constructor, which does not provide a comment
[WARNING] public class CodeBench_jmhType_B3 extends CodeBench_jmhType_B2 {
[WARNING] ^
[WARNING] 78 warnings
[INFO] Building jar: /home/runner/work/FSE_Wrapper/FSE_Wrapper/target/FSE_Wrapper-0.1.1-javadoc.jar
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  10.040 s
[INFO] Finished at: 2026-07-31T23:29:34Z
[INFO] ------------------------------------------------------------------------
no main manifest attribute, in target/FSE_Wrapper-0.1.1-javadoc.jar
Error: Process completed with exit code 1.
```
5. Could we kick JMH out of the jars we build for maven publishing altogehter? JMH bloats the jar file A LOT. Also, JMH puts a lot of warnings into the javadoc, which is not really necessary. I would like to have a way to suppress those warnings. Perhaps we could build an own jar for performance testing?
6. In the maven Jars, the native binaries are completely missing as far as I can tell. Is there a standard for including them into jars or should they always be downloaded seperately? Which would be ok to me...
7. The benchmark is better then before, but it has the following quirks:
- It is showing very big numbers for the longer data which seems not to be correct
- The Medium entropy data is too short, in the end we need a text block which is larger. Perhaps take one from wikipedia or so, in any case we need more words and perhaps mix in numbers.
- The benchmark is not valid in absolute numbers, since the runners are thin machines... Maybe you can create a script which I can run locally an commit the results into the release? Then we have the runner as verification and backup?
- the results of panama, java and .net still not showing up.
